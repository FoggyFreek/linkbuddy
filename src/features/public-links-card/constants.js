// The card's padding, in px, by breakpoint: `LinksCard` spends it as the Card's
// `p`, `BandBanner` negates it to bleed the banner edge-to-edge. They only stay
// in sync by both reading it here.
export const CARD_PAD_TOP = { xs: 20, sm: 30 }
export const CARD_PAD_X = { xs: 14, sm: 28 }
